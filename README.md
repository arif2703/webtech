# Web Tech Project

## Git Commands

### Create a Branch
```bash
$ git checkout -b "<branchName>"
```

### merge other branch
```bash
$ git pull
$ git merge origin/master

$ git add .
$ git commit -m "resolved conflict"
$ git push
```